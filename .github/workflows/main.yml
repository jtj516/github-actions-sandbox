on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      #- name: Checkout
      #  uses: actions/checkout@v3
      - name: Run the scripts and don't ask questions
        uses: jtj516/github-actions-sandbox@v0.0.6
        id: hello
        with:
          scripts: 'Mona the Octocat'